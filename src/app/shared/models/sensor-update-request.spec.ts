import { SensorUpdateRequest } from './sensor-update-request';

describe('SensorUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new SensorUpdateRequest()).toBeTruthy();
  });
});
