# AmbientZero.Web WIP
The web interface for the AmbientZero Project was made with Angular, [ApexCharts](https://apexcharts.com/angular-chart-demos/ "ApexCharts"), and [Bootstrap](https://ng-bootstrap.github.io/#/home "Bootstrap")

## Build Locally

TBD

## Build For Deploy

To deploy manually into an RPI you should execut the following command  `ng build --configuration production` and then look inside the folder `/dist/browser/` and copy the contents to the folder that you are using with your HTTP Server

## Installation And Setup Instructions

A step-by-step guide is coming


